-- ⚠️ 초안만 작성 — 실행 전 대표 승인 필수(하드룰: DB 실행 금지, SQL 생성만).
-- 기존 테이블 컬럼 추가만(신규 테이블 없음) — weekly_details 별도 테이블 생성하지 않음.
--
-- 용도: 주간 리포트 "상세"를 weekly_summaries(기존=주간 요약)의 같은 행에 컬럼으로 공존시킨다.
--   요약과 상세는 같은 주(週) 단위 리포트를 요약본/상세본으로 나눈 것뿐이라 1:1로 항상
--   붙어다니므로 별도 테이블로 분리하지 않고 한 행에 두는 것이 데이터 정합성상 자연스럽다.
-- ================================================================

ALTER TABLE weekly_summaries
  ADD COLUMN detail_text TEXT,
  ADD COLUMN detail_dashboard_cards JSONB;

-- 기존 행은 NULL 유지(과거 주간 요약 백필 안 함) — 신규 생성분부터만 상세가 채워진다.
